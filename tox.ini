[tox]
envlist = py27,py33

[testenv]
changedir = {envsitepackagesdir}/seqlearn
install_command = {toxinidir}/install-deps.py {packages}
deps =
    conda
    nose
    cython
    numpy
    scipy
    scikit-learn
commands =
    pip install -I {toxinidir}
    nosetests --exe {toxinidir}/seqlearn/tests
